const success = (data) => ({
    type: "DATA_SET",
    task: data
  })

  const loading = (status) => ({
    type: "LOADING",
    task: status
  })

  const pokeStates = () => ({
    type: "POKE_STATES",
  })

const filter = () => {
  
    return dispatch => {
      
  
    } 
}
 
export default filter;